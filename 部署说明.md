# 部署说明 · QQ2008 行动面板（frontend/index.js）

本文件是「重生2008年北京筒子楼」改3卡 的可操作行动面板前端。**单文件、零依赖**，托管在任何静态位置即可工作。

---

## 一、三步部署

### 第 1 步：上传 `frontend/index.js`

把它传到任意可直链的静态托管，任选一种：

| 方式 | 说明 | 最终直链形如 |
|---|---|---|
| GitHub 仓库（推荐） | push 到仓库根目录或任意目录（如 `frontend/`） | `https://raw.githubusercontent.com/<you>/<repo>/main/frontend/index.js` |
| jsDelivr（加速） | GitHub 上传后自动获得 | `https://cdn.jsdelivr.net/gh/<you>/<repo>@main/frontend/index.js` |
| Gitee Pages / 个人服务器 | 任意静态服务 | `https://你的域名/路径/index.js` |
| 本地测试 | 见文末「本地测试」 | `http://localhost:5500/frontend/index.js` |

> 当前卡内默认指向 `zhamuqiu/tongren2` 仓库的 `frontend/index.js`（与本卡图床同仓）。
> 若你把它传到别处，执行第 2 步修改 URL 即可。

### 第 2 步：改卡内注入器的 URL（仅当你换了托管位置时需要）

用文本编辑器打开「重生2008年北京筒子楼_改3_成品.json」，找到：

```
"scriptName": "面板注入-QQ2008侧边栏",
```

在其上方不远处的 `"replaceString"` 里，有引导脚本开头的一块：

```js
var urls=[
  'https://cdn.jsdelivr.net/gh/1134189025/tongzilou-frontend@main/frontend/index.js',   // 源1（jsDelivr，国内较快）
  'https://raw.githubusercontent.com/1134189025/tongzilou-frontend/main/frontend/index.js' // 源2（备份）
];
```

把这两行换成你自己的直链即可。**不改也能用**（当前默认指向你自己的 `tongzilou-frontend` 仓库）。

> 💡 **内联兜底**：本卡的正则注入器已内置「外链优先 + 内联完整回退」。即使两条外链全部失效、被防火墙拦截、或被 ST 清洗外链脚本，卡内自带的一份完整面板代码会自动执行，面板照常渲染。因此**只要导入成品卡，面板就一定能出现**；外链只是在发布新版本时用来自动更新。

### 第 3 步：导入卡片

1. 把「重生2008年北京筒子楼_改3_成品.json」拖进 SillyTavern 导入；
2. 新开聊天（或点开开场消息列表选择「2008 QQ 上线」）；
3. 右下角出现 🐧 侧边栏 = 成功。

旧卡「改2」不受影响，可随时换回。

---

## 二、工作原理（速览）

```
AI 消息里出现 <tongzilou_panel />
        │  （世界书《前端面板集成》约束 AI 定期输出）
        ▼
正则「面板注入」把占位符替换成引导脚本（挂载 div + 双策略加载）
        ▼
引导脚本：先尝试从你的仓库加载 frontend/index.js
        ├─ 成功 → 用外链版（后续更新自动生效）
        └─ 失败 / 超时 / 被拦截 → 立即执行卡内内联的完整面板代码
        ▼
右下角挂载 QQ2008 侧边栏 → 解析最近一条 AI 消息的
<Status_block> / <vintage_post> → 显示时间/地点/金钱/
心情/在场角色/每人的攻略阶段
        ▼
点按钮 → 自动向输入框填入【行动】+文案 → 发送
        ▼
AI 收到【行动】按正常剧情推进（合理性审查照旧）
```

---

## 三、功能清单

- **此刻推荐**：每条 AI 回复后自动刷新，按「时间 × 地点 × 在场」给出 3 个行动
- **好友页**（半透攻略）：8 人分 5 组，点开看到「当前阶段名 + 该阶段可做的事 + 2 个推荐行动按钮」；在线状态点由心情值映射
- **目标页**：短线 3 / 中线 3 / 长线 3，点击直达行动
- **行动页**：36 个日常行动，按地点分组
- **说明页**：新手必读（怎么玩、三条规矩、召唤方法）
- **收起/藏起**：标题栏 – 收起（可点 🐧 圆钮再展开）、× 彻底隐藏

---

## 四、故障排查

| 现象 | 原因与处理 |
|---|---|
| 右下角没有企鹅 | ① 导入的不是成品卡/消息没带 `<tongzilou_panel />` → 在输入框打 **面板** 触发 AI 输出占位符；② 内联兜底也没执行（极少数环境连内联脚本都拦）→ 消息处应出现红色错误条「面板加载失败」，把浏览器 F12 控制台报错发回给我 |
| 面板是旧的（外链版） | jsDelivr 有缓存（几分钟~24h）→ 等一会或给 URL 加版本号 `@main` 不变、可加 `?v=2` 强刷 |
| 有面板但状态栏空白 | 当前消息没有 `<Status_block>` / `<vintage_post>`（旧消息）→ 发一条消息让 AI 按格式回，自动刷新 |
| 点按钮没发送 | 输入框 id 变了（第三方主题/插件）→ 面板会提示「没找到输入框」；用原版 ST 皮肤即可；另一可能是你的环境禁用了脚本注入（浏览器设置） |
| 面板重复出现 | 正常现象：每条带占位符的消息会重新执行脚本，脚本有单例保护，只保留一个面板并自动刷新 |
| 图片头像不显示 | 图床 404 → 自动回退为首字头像，不影响功能 |
| 面板挡住聊天 | 标题栏 – 收起成小圆钮，或 × 隐藏；要找回就打「面板」 |

---

## 五、本地测试（部署前先用）

```bash
# 在项目根目录启动一个静态服务
npx serve .        # 或
python -m http.server 5500   # 或任意静态服务器
```

然后把卡内 `replaceString` 两条 URL 临时改成：

```js
var urls=['http://localhost:5500/frontend/index.js'];
```

导入改3卡 → 新开聊天 → 面板应出现在右下角。测完记得改回正式 URL。

---

## 六、卸载 / 回退

换回「改2」卡即可完整回退。若想在改3 上禁用面板，只需两步：

1. 删除 regex_scripts 里的「面板注入-QQ2008侧边栏」条目；
2. 删除世界书条目「前端面板集成」（可选：保留也不影响，只是 AI 还会输出占位符，但没正则渲染，会显示成一行纯文本标签）。

---

## 七、安全性说明

- 面板按钮**只提供 SFW 日常/陪伴行动**；不绕过《合理性审查》《性行为底线约束》《意图锁定原则》。
- 【行动】前缀只是「剧情内行动指令」的标记，AI 仍按角色意愿正常拒绝或同意。
- 前端不收集、不发送任何个人信息；所有数据来自本地聊天上下文。